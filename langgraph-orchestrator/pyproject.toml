[project]
name = "langgraph-orchestrator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-openai>=1.6.1",
    "langgraph>=1.2.11",
    "mcp-crew>=0.1.0",
    "streamlit>=1.63.0",
]

[tool.uv.sources]
mcp-crew = { path = "../mcp-crew" }
